import { MitarbeiterEmailPipe } from './mitarbeiter-email.pipe';

describe('MitarbeiterEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new MitarbeiterEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
