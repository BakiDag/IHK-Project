import { MitarbeiterRolePipe } from './mitarbeiter-role.pipe';

describe('MitarbeiterRolePipe', () => {
  it('create an instance', () => {
    const pipe = new MitarbeiterRolePipe();
    expect(pipe).toBeTruthy();
  });
});
