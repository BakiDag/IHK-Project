import { MitarbeiterVornamePipe } from './mitarbeiter-vorname.pipe';

describe('MitarbeiterVornamePipe', () => {
  it('create an instance', () => {
    const pipe = new MitarbeiterVornamePipe();
    expect(pipe).toBeTruthy();
  });
});
