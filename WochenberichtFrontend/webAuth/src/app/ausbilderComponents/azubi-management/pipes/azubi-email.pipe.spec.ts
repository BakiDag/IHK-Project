import { AzubiEmailPipe } from './azubi-email.pipe';

describe('AzubiEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new AzubiEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
