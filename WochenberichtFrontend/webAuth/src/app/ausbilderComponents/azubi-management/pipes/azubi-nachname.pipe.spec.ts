import { AzubiNachnamePipe } from './azubi-nachname.pipe';

describe('AzubiNachnamePipe', () => {
  it('create an instance', () => {
    const pipe = new AzubiNachnamePipe();
    expect(pipe).toBeTruthy();
  });
});
