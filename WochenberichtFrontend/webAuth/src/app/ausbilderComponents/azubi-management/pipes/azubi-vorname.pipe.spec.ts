import { AzubiVornamePipe } from './azubi-vorname.pipe';

describe('AzubiVornamePipe', () => {
  it('create an instance', () => {
    const pipe = new AzubiVornamePipe();
    expect(pipe).toBeTruthy();
  });
});
